# Adapters

An adapter is essentially a pair of functions: a verification function, and a conversion function.
The verification function takes a path as an argument and returns whether or not the file